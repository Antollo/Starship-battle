{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build with cmake and run",
            "type": "shell",
            "command": "cmake . ; cmake --build . --config Release ; Start-Process -FilePath ./src/Release/starship_battle_game.exe -ArgumentList \"server\" -WorkingDirectory ./src/Release ; Start-Process -FilePath ./src/Release/starship_battle_game.exe -ArgumentList \"127.0.0.1\", \"1717\" -WorkingDirectory ./src/Release",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
